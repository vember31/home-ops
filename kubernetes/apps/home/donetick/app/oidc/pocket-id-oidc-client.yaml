---
# yaml-language-server: $schema=https://raw.githubusercontent.com/aclerici38/pocket-id-operator/v0.8.0/dist/schemas/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: donetick
  namespace: home
spec:
  name: Donetick
  instanceSelector:
    matchLabels:
      app.kubernetes.io/name: pocket-id
  callbackUrls:
    - https://${GATUS_SUBDOMAIN:-${APP}}.${SECRET_DOMAIN}/auth/oauth2
  logoutCallbackUrls:
    - https://${GATUS_SUBDOMAIN:-${APP}}.${SECRET_DOMAIN}
  launchUrl: https://${GATUS_SUBDOMAIN:-${APP}}.${SECRET_DOMAIN}
  pkceEnabled: true
  allowedUserGroups:
    - name: donetick-users
      namespace: security
  secret:
    name: donetick-oidc-credentials
    additionalLabels:
      app.kubernetes.io/name: donetick
