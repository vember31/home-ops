---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name tauticord
  namespace: media

spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system

  install:
    timeout: 10m
    replace: true
    crds: CreateReplace
    createNamespace: true
    remediation:
      retries: 3

  upgrade:
    remediation:
      remediateLastFailure: true
      retries: 3
      strategy: rollback
    cleanupOnFail: true
    crds: CreateReplace

  test:
    enable: true

  rollback:
    recreate: true
    cleanupOnFail: true

  uninstall:
    keepHistory: false
    
  maxHistory: 1

  values:

    controllers:
      ${APP}:
        enabled: true
        type: deployment
        annotations:
          reloader.stakater.com/auto: "true"
        replicas: 1

        containers:
          app:
            image:
              repository: codeberg.org/nwithan8/tauticord
              tag: 6.0.1@sha256:470f99de894e3d93e08ed0f7f505e54a102ba56a90fd39f86043a63a22aeefd7
              pullPolicy: IfNotPresent

            lifecycle:
              postStart:
                exec:
                  command: ["/bin/sh", "-c", "cp /template/tauticord.yaml /config/tauticord.yaml"]


            resources:
              limits:
                memory: 512Mi
              requests:
                cpu: 10m

    persistence:

      config:
        enabled: true
        type: secret
        name: tauticord-secret
        globalMounts:
          - path: /template/tauticord.yaml
            subPath: tauticord.yaml
            readOnly: true

      logs:
        enabled: true
        type: emptyDir
