---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "flux-source-controller-gatus-ep"
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "Flux Source Controller"
        group: internal-backend
        url: "tcp://source-controller.flux-system.svc.cluster.local:9090"
        interval: 1m
        conditions:
          - "[CONNECTED] == true"
        alerts:
          - type: discord
            send-on-resolved: true
            failure-threshold: 2