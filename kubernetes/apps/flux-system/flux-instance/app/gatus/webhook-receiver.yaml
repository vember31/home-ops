---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "flux-webhook-receiver-gatus-ep"
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "Flux Webhook Receiver"
        group: internal-backend
        url: "tcp://webhook-receiver.flux-system.svc.cluster.local"
        interval: 1m
        conditions:
          - "[CONNECTED] == true"
        alerts:
          - type: discord
            send-on-resolved: true
            failure-threshold: 2