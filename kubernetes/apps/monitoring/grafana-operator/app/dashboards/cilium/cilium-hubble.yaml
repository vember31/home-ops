---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: grafana-dash-cilium-hubble
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folderRef: grafana-folder-cilium
  resyncPeriod: 30m
  # renovate: depName="Cilium v1.12 Hubble Metrics"
  grafanaCom:
    id: 16613
    revision: 1
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: VictoriaMetrics
